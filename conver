Yes, this suggestion is reasonable, but I would not call it the guaranteed “best solution.” I’d call it the best low-cost next step after what we found.

What I agree with

Your latest data shows:

Plain Text
1
Cold-ish request: 6 to 8 sec
2
Warm request: ~0.6 sec
Show more lines

So keeping the Lambda and DB side warm can absolutely help.

Warm logs looked excellent:

Plain Text
1
IAM token: ~5 ms
2
DB connection: ~92 to 138 ms
3
Query execution: ~200 ms
4
Lambda duration: ~300 to 350 ms
5
Total response: ~0.6 sec
Show more lines

So yes, a warmer makes sense because the warm path is already under the 2 sec SLA.

What I’d be careful with

This line is a little too confident:

Plain Text
1
Expected result: cold calls land around 0.5 to 1.0s
Show more lines

I’d say:

Plain Text
1
Expected result: normal calls should behave closer to the warm path, likely under 2s, but not guaranteed.
Show more lines

Because EventBridge warmer is still best effort. AWS can recycle Lambda containers, deployments create new containers, and concurrent requests can still create new cold environments.

Best practical approach

I agree with this order:

Plain Text
1
1. Keep RDS Proxy
2
2. Add EventBridge warmer
3
3. Add warmup path in Lambda
4
4. Increase Aurora min ACU to 2
5
5. If SLA still needs hard guarantee, use Provisioned Concurrency
Show more lines
Why this is better than only Provisioned Concurrency

Provisioned Concurrency keeps Lambda warm, but it does not directly keep Aurora/cache/backend query path warm.

Your warm result proves both Lambda and DB/query path behave well once hot. So warming both layers makes sense:

Plain Text
1
EventBridge warmer = keeps Lambda active
2
SELECT 1 touch = keeps DB/proxy path active
3
Aurora min ACU 2 = keeps Aurora from scaling too low
Show more lines
What I’d send to the team
Plain Text
1
This makes sense as the next low-cost step. RDS Proxy is working and warm calls are now around 600 ms, but cold or first-hit behavior is still higher. I think we should keep RDS Proxy, add an EventBridge warmer every 5 minutes, have the Lambda short-circuit on a warmup payload and do a light SELECT 1 DB touch, and bump Aurora min capacity from 1 to 2. That should keep both the Lambda runtime and Aurora path warm without jumping straight to Provisioned Concurrency cost. If this still doesn’t consistently keep us under 2 seconds, then Provisioned Concurrency would be the next stronger option.
Show more lines
My recommendation

Do not remove RDS Proxy.
 Do yes to the warmer plus Aurora min capacity bump.
 Treat Provisioned Concurrency as the next step only if warmer plus min ACU still doesn’t satisfy the SLA.
