Policy graph ingest: debug logging is working. DEBUG lines show up from main and mapper.personal_auto_mapper. In the console, filter the log group with { $.level = "DEBUG" } and set the time range to cover Sept 20.

SOAP verification: no invocations in the last two days. The logs only show the provisioned concurrency warm-up entries, so there's nothing to log yet. Debug lines should show up once IVS starts sending calls.
