PS C:\Users\T012sp\Git repo\tf-aws-aurorapostgres-infra> cd "C:\Users\T012sp\Git repo\tf-aws-aurorapostgres-infra"
PS C:\Users\T012sp\Git repo\tf-aws-aurorapostgres-infra> 
PS C:\Users\T012sp\Git repo\tf-aws-aurorapostgres-infra> git diff
diff --git a/apps/realtimeinsurance/envs/dev.tfvars b/apps/realtimeinsurance/envs/dev.tfvars
index 0f76202..7b444df 100644
--- a/apps/realtimeinsurance/envs/dev.tfvars
+++ b/apps/realtimeinsurance/envs/dev.tfvars
@@ -18,6 +18,7 @@ storage_encrypted                    = false
 aurora_security_group_description    = "Allows access for developers and applications to connect"
 rds_proxy_security_group_description = "RDS Proxy: inbound from Lambda, outbound to Aurora"
 rds_proxy_application_secret_arn     = "arn:aws:secretsmanager:us-east-1:058264370150:secret:realtimeinsurance-NNrdwU"
+rds_proxy_application_username = "pgsi_ans04703015"
 # Must match the live security groups' descriptions exactly - changing these
 # forces destroy+recreate of the real SGs (description is ForceNew).
 
diff --git a/apps/realtimeinsurance/envs/prod.tfvars b/apps/realtimeinsurance/envs/prod.tfvars
index 12f1652..bfe4da8 100644
--- a/apps/realtimeinsurance/envs/prod.tfvars
+++ b/apps/realtimeinsurance/envs/prod.tfvars
@@ -11,7 +11,7 @@ replicas               = 1
 cluster_identifier     = "pol-db-rds-cluster-prod-realtimeins-pg"
 business_ops_category  = "POL"
 requested_by           = "HC-78251"
-
+rds_proxy_application_username = "pgsp_aps04703014"
 
 ##backup configurations
 
diff --git a/apps/realtimeinsurance/main.tf b/apps/realtimeinsurance/main.tf
index b8b885d..15c36f5 100644
--- a/apps/realtimeinsurance/main.tf
+++ b/apps/realtimeinsurance/main.tf
@@ -76,8 +76,5 @@ module "aurora" {
   # auto-managed master user secret.
   enable_rds_proxy = true
 
-  rds_proxy_application_auth = {
-    username   = "pgsi_ans04703015"
-    secret_arn = var.rds_proxy_application_secret_arn
-  }
+  rds_proxy_application_username = var.rds_proxy_application_username
 }
\ No newline at end of file
diff --git a/apps/realtimeinsurance/variables.tf b/apps/realtimeinsurance/variables.tf
index 2335672..3c970da 100644
--- a/apps/realtimeinsurance/variables.tf
+++ b/apps/realtimeinsurance/variables.tf
@@ -187,4 +187,11 @@ variable "rds_proxy_application_secret_arn" {
   description = "Secrets Manager ARN for the RDS Proxy application user."
   type        = string
   default     = null
-}
\ No newline at end of file
+}
+
+variable "rds_proxy_application_username" {
+  description = "Database username for the RDS Proxy application user. Null disables the application auth block for this environment."
+  type        = string
+  default     = null
+  nullable    = true
+}
diff --git a/modules/aurora-postgres/outputs.tf b/modules/aurora-postgres/outputs.tf
index 7efbc9e..ad59146 100644
--- a/modules/aurora-postgres/outputs.tf
+++ b/modules/aurora-postgres/outputs.tf
@@ -47,3 +47,8 @@ output "master_username" {
   description = "Master username configured on the cluster - the DB user the RDS Proxy authenticates connections as."
   value       = var.master_username
 }
+
+output "rds_proxy_application_secret_arn" {
+  description = "ARN of the RDS Proxy application user secret, for callers granting read access. Null when no application user is configured."
+  value       = local.app_auth_enabled ? aws_secretsmanager_secret.rds_proxy_application[0].arn : null
+}
\ No newline at end of file
diff --git a/modules/aurora-postgres/rds_proxy.tf b/modules/aurora-postgres/rds_proxy.tf
index a38253f..2059512 100644
--- a/modules/aurora-postgres/rds_proxy.tf
+++ b/modules/aurora-postgres/rds_proxy.tf
@@ -8,6 +8,21 @@
 data "aws_caller_identity" "current" {}
 data "aws_region" "current" {}
 
+locals {
+  app_auth_enabled = var.enable_rds_proxy && var.rds_proxy_application_username != null
+}
+
+resource "aws_secretsmanager_secret" "rds_proxy_application" {
+  count = local.app_auth_enabled ? 1 : 0
+
+  name        = "${var.application_name}-${var.env_tier}-rds-proxy-app-user"
+  description = "RDS Proxy application user credentials for ${var.application_name} ${var.env_tier}. Created empty by Terraform; value populated out-of-band."
+
+  tags = merge(var.tags, {
+    Name = "${var.application_name}-${var.env_tier}-rds-proxy-app-user"
+  })
+}
+
 module "rds_proxy_security_group" {
   count = var.enable_rds_proxy ? 1 : 0
 
@@ -66,8 +81,8 @@ data "aws_iam_policy_document" "rds_proxy_secret_access" {
 
     resources = concat(
       [aws_rds_cluster.aurora.master_user_secret[0].secret_arn],
-      var.rds_proxy_application_auth != null ? [
-        var.rds_proxy_application_auth.secret_arn
+      local.app_auth_enabled ? [
+        aws_secretsmanager_secret.rds_proxy_application[0].arn
       ] : []
     )
   }
@@ -80,8 +95,8 @@ data "aws_iam_policy_document" "rds_proxy_secret_access" {
       [
         "arn:aws:rds-db:${data.aws_region.current.name}:${data.aws_caller_identity.current.account_id}:dbuser:${aws_rds_cluster.aurora.cluster_resource_id}/${var.master_use
rname}"
       ],
-      var.rds_proxy_application_auth != null ? [
-        "arn:aws:rds-db:${data.aws_region.current.name}:${data.aws_caller_identity.current.account_id}:dbuser:${aws_rds_cluster.aurora.cluster_resource_id}/${var.rds_proxy_
application_auth.username}"
+      local.app_auth_enabled ? [
+        "arn:aws:rds-db:${data.aws_region.current.name}:${data.aws_caller_identity.current.account_id}:dbuser:${aws_rds_cluster.aurora.cluster_resource_id}/${var.rds_proxy_
application_username}"
       ] : []
     )
   }
@@ -114,12 +129,12 @@ resource "aws_db_proxy" "aurora" {
   }
 
   dynamic "auth" {
-    for_each = var.rds_proxy_application_auth != null ? [var.rds_proxy_application_auth] : []
+    for_each = local.app_auth_enabled ? [1] : []
 
     content {
       auth_scheme = "SECRETS"
       iam_auth    = "REQUIRED"
-      secret_arn  = auth.value.secret_arn
+      secret_arn  = aws_secretsmanager_secret.rds_proxy_application[0].arn
     }
   }
 
@@ -156,4 +171,4 @@ resource "aws_db_proxy_endpoint" "aurora_reader" {
   vpc_subnet_ids         = var.aurora_subnet_ids
   vpc_security_group_ids = [module.rds_proxy_security_group[0].sg_id]
   target_role            = "READ_ONLY"
-}
+}
\ No newline at end of file
diff --git a/modules/aurora-postgres/variables.tf b/modules/aurora-postgres/variables.tf
index 5590ddc..12cd560 100644
--- a/modules/aurora-postgres/variables.tf
+++ b/modules/aurora-postgres/variables.tf
@@ -162,4 +162,11 @@ variable "rds_proxy_application_auth" {
 
   default  = null
   nullable = true
+}
+
+variable "rds_proxy_application_username" {
+  description = "Database username for the application user on the RDS Proxy. When set, the module creates an empty Secrets Manager secret for it and attaches a second auth
 block to the proxy. The secret value must be populated out-of-band before the app user can connect."
+  type        = string
+  default     = null
+  nullable    = true
 }
\ No newline at end of file
(END)
@@ -162,4 +162,11 @@ variable "rds_proxy_application_auth" {
 
   default  = null
   nullable = true
+}
+
+variable "rds_proxy_application_username" {
+  description = "Database username for the application user on the RDS Proxy. When set, the module creates an empty Secrets Manager secret for it and attaches a second auth block to the proxy. The secret value must be populated out-of-band before the app user can connect."
+  type        = string
+  default     = null
+  nullable    = true
 }
\ No newline at end of file
~
~
~
~
~
~
~
~
~
~
~
~
~
~
~
~
~
~
~
~
~
~
~
~
~
~
~
~
~
~
~
~
PS C:\Users\T012sp\Git repo\tf-aws-aurorapostgres-infra> tofu fmt -check -recursive
apps\realtimeinsurance\envs\dev.tfvars
apps\realtimeinsurance\envs\prod.tfvars
PS C:\Users\T012sp\Git repo\tf-aws-aurorapostgres-infra> tofu validate
Success! The configuration is valid.
PS C:\Users\T012sp\Git repo\tf-aws-aurorapostgres-infra>
