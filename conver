Hey Alex — found the PERF 403. Guidewire sends Entra v1 tokens (sts.windows.net issuer, bare GUID audience) but the authorizer only accepted the v2 formats. We hit this same thing in DEV a while back and I fixed it in the console instead of in code, which is why PERF broke the same way — trying to make it stick this time. Change makes JWT_ISSUER accept a comma-separated list like JWT_AUDIENCE already does, so both formats work. Would you do it differently? The alternative is pushing Guidewire to send v2 tokens and keeping the authorizer strict, but that's an app registration change affecting everything that uses it. Happy to hold off if you'd rather go that route.
