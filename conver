The role looks good — trust policy and permissions are both correct. Turns out there's one more bit though: API Gateway doesn't pick it up automatically, the account settings have to point at it explicitly. Could you run `aws apigateway update-account --patch-operations op=replace,path=/cloudwatchRoleArn,value=arn:aws:iam::058264307873:role/apigateway-cloudwatch-role` in us-east-1? I'd do it myself but I don't have apigateway:PATCH. Our apply is still failing on the same error until that's set.
