aws ec2 describe-security-groups `
  --filters "Name=tag:Name,Values=*realtimeins*" `
  --query "SecurityGroups[].[GroupId,GroupName,Description]" --output table


$env:AWS_PROFILE = "<prod-profile>"
aws sts get-caller-identity
