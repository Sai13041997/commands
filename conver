cd "C:\Users\T012sp\Git repo\tf-aws-aurorapostgres-infra"
git branch --show-current

# IAM-only app user (rds_iam) for the RDS Proxy
rds_proxy_application_username = "pgsi_ans04703015"

tofu fmt -recursive
git add apps/realtimeinsurance/envs/perf.tfvars apps/realtimeinsurance/envs/staging.tfvars
git commit -m "Add app user for perf and staging"
git push
