Thanks CG, the connection works now. One more step: pgsi_ans04703015 can connect and reach the ods schema, but gets permission denied for table policy when it tries to write. Could you grant it SELECT, INSERT, UPDATE and DELETE on the tables in ods, and USAGE on the sequences? Worth setting default privileges too, so new tables are covered automatically. And the same for pgsi_aps04703014 on realtimeinsurance_prod
