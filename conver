cd ..
git log --oneline -3 -- authorizer.zip
Expand-Archive -Path authorizer.zip -DestinationPath "$env:TEMP\authverify" -Force
(Get-FileHash "$env:TEMP\authverify\index.js").Hash
(Get-FileHash authorizerSrc\index.js).Hash
