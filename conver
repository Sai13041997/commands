PS C:\Users\T012sp\Git repo\tf-module-api-gateway> tofu fmt main.tf variables.tf
PS C:\Users\T012sp\Git repo\tf-module-api-gateway> tofu vaildate 
OpenTofu has no command named "vaildate". Did you mean "validate"?

To see all of OpenTofu's top-level commands, run:
  tofu -help

PS C:\Users\T012sp\Git repo\tf-module-api-gateway> tofu validate
╷
│ Warning: Deprecated attribute
│ 
│   on main.tf line 186, in resource "aws_api_gateway_authorizer" "jwt":
│  186:   authorizer_uri                   = "arn:aws:apigateway:${data.aws_region.current.name}:lambda:path/2015-03-31/functions/${aws_lambda_function.jwt_authorizer[0].arn}/invocations"
│ 
│ The attribute "name" is deprecated. Refer to the provider documentation for details.
│ 
│ (and 5 more similar warnings elsewhere)
╵
Success! The configuration is valid, but there were some validation warnings as shown above.
PS C:\Users\T012sp\Git repo\tf-module-api-gateway> git diff
diff --git a/authorizerSrc/index.js b/authorizerSrc/index.js
index 4d743fc..4542774 100644
--- a/authorizerSrc/index.js
+++ b/authorizerSrc/index.js
@@ -21,7 +21,13 @@ function allow(principalId, resource, context) {
 }
 
 exports.handler = async (event) => {
-  const issuer = process.env.JWT_ISSUER; // https://sts.windows.net/<tenant>/
+  // Accepts one or more comma-separated issuers, mirroring JWT_AUDIENCE.
+  // Entra v1 tokens use https://sts.windows.net/<tenant>/ and v2 tokens use
+  // https://login.microsoftonline.com/<tenant>/v2.0
+  const issList = (process.env.JWT_ISSUER || "")
+    .split(",")
+    .map((s) => s.trim())
+    .filter(Boolean);
   const audList = (process.env.JWT_AUDIENCE || "")
     .split(",")
     .map((s) => s.trim())
@@ -36,7 +42,15 @@ exports.handler = async (event) => {
 
   const token = m[1];
 
-  const tenantMatch = issuer.match(/sts\.windows\.net\/([^/]+)\//i);
+  // Tenant ID appears in both issuer formats; match either so the JWKS URI is
+  // always tenant-scoped rather than falling back to /common.
+  const tenantMatch = issList
+    .map((iss) =>
+      iss.match(
+        /(?:sts\.windows\.net|login\.microsoftonline\.com)\/([0-9a-f-]{36})/i
+      )
+    )
+    .find(Boolean);
   const tenantId = tenantMatch ? tenantMatch[1] : null;
 
   // FIX: use v2.0 keys endpoint (matches your well-known config jwks_uri)
@@ -67,7 +81,7 @@ exports.handler = async (event) => {
         token,
         getKey,
         {
-          issuer,
+          issuer: issList.length ? issList : undefined,
           audience: audList.length ? audList : undefined,
           algorithms: ["RS256"],
           clockTolerance: 60,
@@ -99,7 +113,7 @@ exports.handler = async (event) => {
       message: e?.message,
       jwksUri,
       headerKid,
-      issuer,
+      issuers: issList,
       audiences: audList,
       now: new Date().toISOString(),
     });
diff --git a/main.tf b/main.tf
index 072926f..90f07bd 100644
--- a/main.tf
+++ b/main.tf
@@ -8,8 +8,8 @@ terraform {
 }
 
 locals {
-  # Hardcoded issuer
-  jwt_issuer = "https://sts.windows.net/50bb1d9e-7174-4e06-aecd-e87b1b8c211d/"
+  # Joined for the authorizer, which splits JWT_ISSUER on commas like JWT_AUDIENCE.
+  jwt_issuer = join(",", var.jwt_issuer)
 
   routes_by_key = {
     for r in var.routes :
diff --git a/variables.tf b/variables.tf
index 296de22..48100ad 100644
--- a/variables.tf
+++ b/variables.tf
@@ -114,4 +114,13 @@ variable "execution_logging_level" {
     condition     = contains(["OFF", "ERROR", "INFO"], var.execution_logging_level)
     error_message = "execution_logging_level must be one of OFF, ERROR, or INFO."
   }
+}
+
+variable "jwt_issuer" {
+  type        = list(string)
+  description = "Accepted JWT issuers. Entra v1 tokens use sts.windows.net; v2 tokens use login.microsoftonline.com/<tenant>/v2.0."
+  default = [
+    "https://sts.windows.net/50bb1d9e-7174-4e06-aecd-e87b1b8c211d/",
+    "https://login.microsoftonline.com/50bb1d9e-7174-4e06-aecd-e87b1b8c211d/v2.0",
+  ]
 }
\ No newline at end of file
(END)
