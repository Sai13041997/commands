Open lambda.tf and change only this line:
Find:
hclResource = ["arn:aws:rds-db:us-east-1:${var.destination_account_id}:dbuser:${aws_db_proxy.aurora.resource_id}/lambda_svc"]
Replace with:
hclResource = ["arn:aws:rds-db:us-east-1:${var.destination_account_id}:dbuser:${split(":", aws_db_proxy.aurora.arn)[6]}/lambda_svc"]
Save. Commit, push, re-run plan.What about CG what should I tell her ?Here's a clear message for CG that covers what you need — you can send it tonight so she has it before the meeting, or use it as your script tomorrow morning.

Hey CG,
Quick recap so tomorrow's meeting is fast — here's what I need from you to get RDS Proxy live:
A new Secrets Manager secret containing a static username and password for a Postgres user on the Aurora cluster. Two things about the secret:

It has to be a regular static password (like any service account), not an IAM auth token. RDS Proxy uses the secret to log into Aurora on its own, and IAM tokens can't work for that because they expire in 15 minutes. Secrets Manager rotation on a normal schedule (30/60/90 days) is fine — the proxy handles rotation automatically without downtime.
The user should have the same grants as pgsi_ans00315015 (or lambda_svc — whichever matches what the Lambda currently uses). Not the master account.

Cleanest option is probably a new dedicated user for the proxy (e.g. l_aur_soap_proxy with a password), so we don't touch pgsi_ans00315015's IAM-auth setup. But if it's easier to just add a password to pgsi_ans00315015 alongside its IAM auth, that works too.
What I need from you:

The user created with a static password.
Credentials stored in a new Secrets Manager secret (not the Aurora master secret).
The secret ARN so I can plug it into Terraform.

Everything else on the infra side is ready — as soon as I have the ARN it's a ~15 minute deploy.
Thanks!

A few notes for you, not for the message:

On the "15 minutes" claim — that's the deploy itself, not the total. There's also plan review and getting the PR merged. Just so you aren't over-promising internally.
You may need to gently correct her on the 15-minute point tomorrow. She said in DMs "the password expires every 15 mins, it generates a new one every 15 mins" — that's IAM tokens, not Secrets Manager passwords. The message above tries to explain the difference upfront so it clicks before she starts building anything. If she still pushes back tomorrow, point at the AWS docs for RDS Proxy setup — the secret format is a plain {"username": "...", "password": "..."} JSON. Not a token.
Don't over-explain in the message. I kept it short on purpose. If you make it too long she may skim. The three-item bullet list at the end is what she actually needs to act on.

Send it tonight so she can start on it, or bring it to the morning meeting. Your call.NOOO she will not be in the meeting I need her to create role for arnAh — got it. Then this isn't a "prep for meeting" message, it's a direct ask to unblock you. Sharpen it up:

Hey CG,
Just to make sure I'm not blocked in the morning — could you set up the following before then?
A new Secrets Manager secret with a Postgres user for the RDS Proxy to use. Two important things:

It has to be a static username/password stored in the secret (regular service-account style, like {"username": "...", "password": "..."}). Not an IAM auth token — those expire every 15 minutes and can't be used by the proxy for its own login. Normal Secrets Manager rotation (30/60/90 days) is fine; the proxy handles rotation on its own.
The user should have the same grants as pgsi_ans00315015 — not the master account.

Cleanest is probably a new dedicated user (e.g. l_aur_soap_proxy) with a password, in a new secret. That way we don't touch pgsi_ans00315015's existing IAM-auth setup.
What I need from you:

The user created with a static password.
Credentials in a new Secrets Manager secret (not the Aurora master secret).
Send me the secret ARN.

Everything else is already built and ready to go — I just need the ARN to plug in.
Thanks so much, appreciate it!

Two things to know before you send:

She may push back again on the 15-min token thing. The message tries to head that off with the {"username": ..., "password": ...} example — that shows her exactly what format the secret contents need. If she still comes back with "the password expires every 15 min," she's confusing IAM tokens with Secrets Manager. Ask her to just create a secret with a plain JSON of a username and password she picks. That's it.
Since she's not in the meeting, you have less time to correct misunderstandings in real time. Consider being even more explicit — tell her exactly the JSON structure the secret should have:

json   {
     "username": "l_aur_soap_proxy",
     "password": "SomeStrongPasswordSheGenerates"
   }
That way there's zero ambiguity. If you want, add that to the message before "Cleanest is probably..." — one code block, no room for confusion.
Want me to add that to the draft, or is it clear enough as-is?me:
Hey CG, I'm putting an RDS Proxy in front of realtimeinsurance-dev-aurora-cluster to fix a cold-start issue. The proxy needs its own DB login role (separate from lambda_svc, which the Lambda still uses via IAM through the proxy). Could you spin up a non-person login role l_aur_... naming, password auth (stored in Secrets Manager, KMS + rotation), same priv_* as lambda_svc, NOINHERIT?
CG:
Hey Sai, that’s great! I was actually going to recommend it for the issue you were facing, there’s also something called pg_bouncer that I can add on the postgres side but I’ll look into that if I can have both or the rds proxy is fine.I’ll work on the DB login role and send it to youfor costs sake can you install the pgbouncer instead and we can do the rds proxy for productionMe:
I looked at pg_bouncer too but it'd need its own infra to run on (EC2/ECS) right?, plus patching and HA,  RDS Proxy is basically the managed version of that. I'd lean toward keeping dev and prod on the same path so we're actually testing what prod will do.
CG:
yes you're right. i was trying to set it up but realized that it will need more configurationsi will work on getting the credentials set up for the rds proxyMe:
Great, thanks 
Hey CG, quick heads-up. So we've got a call with KY IVS tomorrow at 11 to test the SOAP call from their side. Any chance you'll have the DB login role + Secrets Manager secret wrapped up by EOD today? Totally understand if not, just want to plan around it. No pressure, just checking!CG:
i am currently working on it rn
Me:
Awesome, you're a savior!CG:
please try this userid :  pgsi_ans00315015
Me:
Sure, thanks Could you also send me the Secrets Manager secret ARN where the pgsi_ans00315015 credentials are stored and the KMS key ARN used to encrypt that secret
CG:
do you have access to the l2-dev accountme:
Yeah but I'm unable to find them 
CG:
this is stored in pgadmin  pgsi_ans00315015secret arn : arn:aws:secretsmanager:us-east-1:058264370150:secret:rds!cluster-de1d9261-65ef-4198-9ae6-73dcb0b25daf-rnMglhMe:
Got it, thanks! Just to double-check on my end, is the pgsi_ans00315015 credential stored in AWS Secrets Manager, or is it saved locally in pgAdmin? Asking because RDS Proxy needs to pull the credentials from Secrets Manager to auth into Aurora, so wanted to make sure I'm looking in the right spot before I wire it up.
CG:
the account  is stored in pgadmin and you copy the secret for the passwordPraneeth, Sai
Got it, thanks! Just to double-check on my end, is the pgsi_ans00315015 credential stored in AWS Secrets Manager, or is it saved locally in pgAdmin? Asking because RDS Proxy needs to pull the credent…
that should workMe:
Bulule, Ntwenhle
7/7/2026 5:19 PM
the account is stored in pgadmin and you copy the secret for the password
Got it, that makes sense!
CG:
please let me know if it worksPraneeth, Sai
Hey CG, quick heads-up. So we've got a call with KY IVS tomorrow at 11 to test the SOAP call from their side. Any chance you'll have the DB login role + Secrets Manager secret wrapped up by EOD today?
do you mind forwarding the meeting invite Me:
Yeah, I forwarded that. Bulule, Ntwenhle
please let me know if it works
I'm working on that I will let you knowCG:
Great, thanks
1 Like reaction.
Me:
Hey CG, I checked and that secret is for kfb_aurora_admin. If we use it, the proxy would end up running as admin which isn't what we want. Could you drop pgsi_ans00315015's password into a new Secrets Manager secret so I can point the proxy at that instead?
CG:
aws rds generate-db-auth-token --hostname realtimeinsurance-dev-aurora-cluster.cluster-cx08iswmk3mr.us-east-1.rds.amazonaws.com --port 5432 --region us-east-1 --username pgsi_ans00315015  --profile l2devYou have to run that command to generate secretWhat aws account profile are you usingMe:
L2-Development Correct me if I'm wrong but I dug into RDS Proxy a bit more and there's a wrinkle. That token from generate-db-auth-token is IAM-based and only lasts 15 min, and it turns out RDS Proxy → Aurora doesn't support IAM auth due to AWS limitation it only takes a static password from Secrets Manager. Good news is the Lambda still uses IAM through the proxy, so IAM auth stays end-user-side. For the proxy's own login, we'd just need a static password stored in Secrets Manager. Two ways to do it: New non-person role with a password, same priv_* as pgsi_ans00315015 or Add a password to pgsi_ans00315015 alongside the IAM authI'm so sorry for disturbing you after the hours CG:
No worries at allPraneeth, Sai
Correct me if I'm wrong but I dug into RDS Proxy a bit more and there's a wrinkle. That token from generate-db-auth-token is IAM-based and only lasts 15 min, and it turns out RDS Proxy → Aurora doesn…
The password from secrets manager expires every 15 mins though, you’re wanting a password that doesn’t expire?Me:
I think the 15-min thing might be from the IAM auth token, that one expires every 15 min. Secrets Manager rotation is different right, you set the schedule yourself 30 days or 90 days, whatever we want right. RDS Proxy handles rotation automatically too, so when Secrets Manager rotates it, the proxy picks up the new password without any downtime. So basically what we need is a normal DB password for pgsi_ans00315015 or a new role, whichever's easier, stored in Secrets Manager on a normal rotation schedule. Same as any regular service account.Does that make sense?CG:
No it expires every 15 mins it generates a new one every 15 minsWe can work on it tomorrow morningMe:
That works for me. Thanks for your time ACCORDING TO THIS CONVERSATIONShow moreAh — got it. Given the actual conversation history, my previous drafts don't fit the situation. Let me recalibrate honestly.
The reality:

She's already agreed "we can work on it tomorrow morning."
She's already twice said the password expires every 15 minutes — she's confidently wrong on this, and she said it after you explained it.
You then said "That works for me" and closed the loop, meaning you're not currently disagreeing with her.
She's not in the KY IVS meeting, but she IS planning to work on it tomorrow morning.

What that means: You don't need a new "please do this" message. She's already committed to working on it tomorrow morning. What you need is a message that (a) confirms the plan for tomorrow, (b) makes the right ask crystal-clear one more time without restarting the debate, and (c) leaves her something concrete she can act on.
Here's a version tuned to that situation:

Hey CG,
Thanks again for the time yesterday. Just wanted to leave you with what I need for tomorrow morning so you have it ready when you sit down to work on it.
I read up more on this last night and want to give it one more shot at explaining, because I don't want us to build the wrong thing and have to redo it:
The 15-minute expiring credential is the IAM auth token (what generate-db-auth-token produces). RDS Proxy → Aurora can't use those, because the proxy has to log into Aurora on its own without regenerating tokens every few minutes.
What Secrets Manager stores is a static password — a fixed string, like any service account password. It doesn't expire on its own. Rotation is scheduled (30/60/90 days, whatever we pick), and RDS Proxy handles the rotation transparently.
So what I actually need is:

A DB user (either pgsi_ans00315015 with a regular password added, or a new one like l_aur_soap_proxy — whichever's easier).
That user's static username + password stored in a new Secrets Manager secret, in this format:

json   {
     "username": "the_user",
     "password": "some_static_password_you_choose"
   }

Send me the ARN of that new secret.

Once I have the ARN it's ~15 min to deploy on my side. Really appreciate the help — sorry to belabor this, just want to be sure we're aligned before you spend time on it.
Thanks!

A few important notes on this version:

It gently re-litigates the 15-min issue. You have to. She said it twice, confidently, and now believes it. If you don't correct it before she starts, she's going to spend her morning trying to generate an IAM token, store it somewhere, and hand it to you — and it won't work. The message frames it as "one more shot at explaining" so it's polite, not condescending. It also grounds the correction in something concrete: the difference between the two things.
The JSON snippet is intentional. That's the exact format Secrets Manager expects for RDS Proxy. If she sees {"username": ..., "password": ...} written out, it becomes hard to interpret it as "IAM token." Concrete beats abstract.
"Sorry to belabor this" — softens the correction without walking it back. You're not saying "you're wrong," you're saying "let me try explaining differently."
You need to send this tonight or first thing in the morning before she starts. If you wait until after she's already tried something at 8am, she'll come to you at 9am saying "here's the token" and you'll be back where you started.

Do you want to send this now (tonight) so she sees it before she starts tomorrow, or wait until first thing in the morning? Honestly — tonight is better. She might read it before bed or over her morning coffee, and you avoid the race condition.Got it but can you make it shorter and more clear pleaseHey CG,
Wanted to leave you with the exact ask for tomorrow so we don't spin wheels:
The 15-min expiring thing is the IAM auth token (generate-db-auth-token). RDS Proxy can't use those — it needs a static password stored in Secrets Manager. Static passwords don't expire; rotation is a scheduled thing (30/60/90 days) that the proxy handles automatically.
What I need:

A DB user with a static password — either add one to pgsi_ans00315015, or create a new one like l_aur_soap_proxy (whichever's easier).
A new Secrets Manager secret with this format:

json   {
     "username": "the_user",
     "password": "any_static_password_you_pick"
   }

Send me the ARN.

Once I have the ARN it's ~15 min to deploy. Thanks!one para pleaseHey CG, wanted to leave you with the exact ask for tomorrow so we don't spin wheels — the 15-min expiring thing is the IAM auth token (generate-db-auth-token), which RDS Proxy can't actually use. It needs a static password stored in Secrets Manager instead. Static passwords don't expire on their own; rotation is a scheduled thing (30/60/90 days) and the proxy handles it automatically. So what I need is: a DB user with a static password (either add one to pgsi_ans00315015 or create a new one like l_aur_soap_proxy, whichever's easier), stored in a new Secrets Manager secret in this format: {"username": "the_user", "password": "any_static_password_you_pick"}, and then send me the ARN. Once I have it, ~15 min to deploy on my side. Thanks!
