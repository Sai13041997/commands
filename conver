Got the timing from the KIVS timeout. The request takes about 5.5 seconds, well past their 2-second limit. The breakdown: IAM token 0.5-1s, DB connect 0.2s, and the query itself 4.7s. So the query is the bottleneck, not cold start. Both requests were the same, and one was already warm.

Probably worth an EXPLAIN ANALYZE on the coverage query. If v_insurer_active_or_not_active is doing heavy joins or there's no index on the VIN, that would explain it.

Separately, we generate a fresh IAM auth token on every request, which costs another 0.5-1s. Caching it for its 15-minute lifetime would claw that back.
