PS C:\Users\T012sp\Git repo\tf-aws-aurorapostgres-infra> tofu fmt -recursive
PS C:\Users\T012sp\Git repo\tf-aws-aurorapostgres-infra> cd .\apps\realtimeinsurance\
PS C:\Users\T012sp\Git repo\tf-aws-aurorapostgres-infra\apps\realtimeinsurance> tofu validate
╷
│ Warning: Deprecated attribute
│ 
│   on ..\..\modules\aurora-postgres\rds_proxy.tf line 81, in data "aws_iam_policy_document" "rds_proxy_secret_access":
│   81:         "arn:aws:rds-db:${data.aws_region.current.name}:${data.aws_caller_identity.current.account_id}:dbuser:${aws_rds_cluster.aurora.cluster_resource_id}/${var.master_username}"
│ 
│ The attribute "name" is deprecated. Refer to the provider documentation for details.
│ 
│ (and one more similar warning elsewhere)
╵
Success! The configuration is valid, but there were some validation warnings as shown above.
PS C:\Users\T012sp\Git repo\tf-aws-aurorapostgres-infra\apps\realtimeinsurance> cd..\..
cd..\.. : The term 'cd..\..' is not recognized as the name of a cmdlet, function, script file, or operable program. Check the spelling of the name, or if a path was 
included, verify that the path is correct and try again.
At line:1 char:1
+ cd..\..
+ ~~~~~~~
    + CategoryInfo          : ObjectNotFound: (cd..\..:String) [], CommandNotFoundException
    + FullyQualifiedErrorId : CommandNotFoundException
 
PS C:\Users\T012sp\Git repo\tf-aws-aurorapostgres-infra\apps\realtimeinsurance> cd ..\..
PS C:\Users\T012sp\Git repo\tf-aws-aurorapostgres-infra> 
PS C:\Users\T012sp\Git repo\tf-aws-aurorapostgres-infra> git diff
diff --git a/apps/realtimeinsurance/envs/dev.tfvars b/apps/realtimeinsurance/envs/dev.tfvars
index e7acdec..a423e2f 100644
--- a/apps/realtimeinsurance/envs/dev.tfvars
+++ b/apps/realtimeinsurance/envs/dev.tfvars
@@ -15,7 +15,6 @@ requested_by                         = "PMOGroup@kyfb.com"
 storage_encrypted                    = false
 aurora_security_group_description    = "Allows access for developers and applications to connect"
 rds_proxy_security_group_description = "RDS Proxy: inbound from Lambda, outbound to Aurora"
-rds_proxy_application_secret_arn     = "arn:aws:secretsmanager:us-east-1:058264370150:secret:realtimeinsurance-NNrdwU"
 rds_proxy_application_username       = "pgsi_ans04703015"
 # Must match the live security groups' descriptions exactly - changing these
 # forces destroy+recreate of the real SGs (description is ForceNew).
diff --git a/apps/realtimeinsurance/variables.tf b/apps/realtimeinsurance/variables.tf
index 3c970da..6d2ca3e 100644
--- a/apps/realtimeinsurance/variables.tf
+++ b/apps/realtimeinsurance/variables.tf
@@ -183,14 +183,8 @@ variable "air_gapped_backup_retention_days" {
   default     = 365
 }
 
-variable "rds_proxy_application_secret_arn" {
-  description = "Secrets Manager ARN for the RDS Proxy application user."
-  type        = string
-  default     = null
-}
-
 variable "rds_proxy_application_username" {
-  description = "Database username for the RDS Proxy application user. Null disables the application auth block for this environment."
+  description = "rds_iam application database user for the RDS Proxy. Null means only the master user is granted IAM access."
   type        = string
   default     = null
   nullable    = true
diff --git a/modules/aurora-postgres/outputs.tf b/modules/aurora-postgres/outputs.tf
index d36a732..947ebea 100644
--- a/modules/aurora-postgres/outputs.tf
+++ b/modules/aurora-postgres/outputs.tf
@@ -48,11 +48,6 @@ output "master_username" {
   value       = var.master_username
 }
 
-output "rds_proxy_application_secret_arn" {
-  description = "ARN of the RDS Proxy application user secret, for callers granting read access. Null when no application user is configured."
-  value       = local.app_auth_enabled ? aws_secretsmanager_secret.rds_proxy_application[0].arn : null
-}
-
 output "rds_proxy_application_username" {
   description = "Application DB username for IAM auth through the RDS Proxy. Null when no application user is configured."
   value       = local.app_auth_enabled ? var.rds_proxy_application_username : null
diff --git a/modules/aurora-postgres/rds_proxy.tf b/modules/aurora-postgres/rds_proxy.tf
index 2059512..c76445b 100644
--- a/modules/aurora-postgres/rds_proxy.tf
+++ b/modules/aurora-postgres/rds_proxy.tf
@@ -1,9 +1,10 @@
 # RDS Proxy for Aurora, gated behind var.enable_rds_proxy.
 #
-# Mirrors real-time-insurance-infra's rds_proxy.tf: end-to-end IAM auth from
-# proxy to Aurora, backed by the cluster's auto-managed master user secret
-# (no separate secret ARN input needed since manage_master_user_password
-# is always on for this module).
+# End-to-end IAM auth: clients connect to the proxy with IAM tokens, and the
+# proxy connects to Aurora with IAM using its role's rds-db:connect grants.
+# The master user authenticates via the cluster's auto-managed master user
+# secret. The optional application user (var.rds_proxy_application_username)
+# is an rds_iam user and needs no secret.
 
 data "aws_caller_identity" "current" {}
 data "aws_region" "current" {}
@@ -12,17 +13,6 @@ locals {
   app_auth_enabled = var.enable_rds_proxy && var.rds_proxy_application_username != null
 }
 
-resource "aws_secretsmanager_secret" "rds_proxy_application" {
-  count = local.app_auth_enabled ? 1 : 0
-
-  name        = "${var.application_name}-${var.env_tier}-rds-proxy-app-user"
-  description = "RDS Proxy application user credentials for ${var.application_name} ${var.env_tier}. Created empty by Terraform; value populated out-of-band."
-
-  tags = merge(var.tags, {
-    Name = "${var.application_name}-${var.env_tier}-rds-proxy-app-user"
-  })
-}
-
 module "rds_proxy_security_group" {
   count = var.enable_rds_proxy ? 1 : 0
 
@@ -79,12 +69,7 @@ data "aws_iam_policy_document" "rds_proxy_secret_access" {
       "secretsmanager:DescribeSecret"
     ]
 
-    resources = concat(
-      [aws_rds_cluster.aurora.master_user_secret[0].secret_arn],
-      local.app_auth_enabled ? [
-        aws_secretsmanager_secret.rds_proxy_application[0].arn
-      ] : []
-    )
+    resources = [aws_rds_cluster.aurora.master_user_secret[0].secret_arn]
   }
 
   statement {
@@ -128,16 +113,6 @@ resource "aws_db_proxy" "aurora" {
     secret_arn  = aws_rds_cluster.aurora.master_user_secret[0].secret_arn
   }
 
-  dynamic "auth" {
-    for_each = local.app_auth_enabled ? [1] : []
-
-    content {
-      auth_scheme = "SECRETS"
-      iam_auth    = "REQUIRED"
-      secret_arn  = aws_secretsmanager_secret.rds_proxy_application[0].arn
-    }
-  }
-
   tags = merge(var.tags, {
     Name = "${var.application_name}-${var.env_tier}-aurora-proxy"
   })
diff --git a/modules/aurora-postgres/variables.tf b/modules/aurora-postgres/variables.tf
index 12cd560..7956f35 100644
--- a/modules/aurora-postgres/variables.tf
+++ b/modules/aurora-postgres/variables.tf
@@ -153,19 +153,8 @@ variable "tags" {
   default     = {}
 }
 
-variable "rds_proxy_application_auth" {
-  description = "Optional application database user authentication for RDS Proxy"
-  type = object({
-    username   = string
-    secret_arn = string
-  })
-
-  default  = null
-  nullable = true
-}
-
 variable "rds_proxy_application_username" {
-  description = "Database username for the application user on the RDS Proxy. When set, the module creates an empty Secrets Manager secret for it and attaches a second auth
 block to the proxy. The secret value must be populated out-of-band before the app user can connect."
+  description = "Optional rds_iam application database user for the RDS Proxy. When set, the proxy role is granted rds-db:connect for this user so it can authenticate end t
o end with IAM. No secret is needed."
   type        = string
   default     = null
   nullable    = true
(END)variable "rds_proxy_application_auth" {
  description = "Optional application database user authentication for RDS Proxy"
  type = object({
    username   = string
    secret_arn = string
  })

  default  = null
  nullable = true
}

variable "rds_proxy_application_username" {
  description = "Optional rds_iam application database user for the RDS Proxy. When set, the proxy role is granted rds-db:connect for this user so it can authenticate end to end with IAM. No secret is needed."
  type        = string
  default     = null
  nullable    = true
}

variable "rds_proxy_application_secret_arn" {
  description = "Secrets Manager ARN for the RDS Proxy application user."
  type        = string
  default     = null
}



variable "rds_proxy_application_username" {
  description = "rds_iam application database user for the RDS Proxy. Null means only the master user is granted IAM access."
  type        = string
  default     = null
  nullable    = true
}


rds_proxy_application_secret_arn     = "arn:aws:secretsmanager:us-east-1:058264370150:secret:realtimeinsurance-NNrdwU"

