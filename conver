PS C:\Users\T012sp\Git repo\tf-aws-aurorapostgres-infra> Get-Content apps\realtimeinsurance\variables.tf | Select-Object -First 8
variable "application_name" {
  description = "Name of application."
  type        = string
  default     = "realtimeinsurance"
}

variable "env_tier" {
  description = "dev, qa, uat, perf, prod, etc."
PS C:\Users\T012sp\Git repo\tf-aws-aurorapostgres-infra>Get-Content apps\realtimeinsurance\variables.tf | Select-Object -First 8Planning to add the prod app-user secret via the module-creates-the-secret approach rather than the external ARN method dev uses, so prod lands on the target state directly. Two things worth your input: CG sent the password in plaintext over chat, so I'd rather have Terraform generate it with random_password than reuse an exposed one — and what naming convention do you want for the secret so the wildcard IAM read works? Any other thoughts before I start?
