Expand-Archive -Path authorizer.zip -DestinationPath "$env:TEMP\authcheck" -Force
Get-ChildItem "$env:TEMP\authcheck" | Select-Object Name, Length
Select-String -Path "$env:TEMP\authcheck\index.js" -Pattern "issList" | Select-Object -First 1


Copy-Item "$env:TEMP\authzip\package-lock.json" authorizerSrc\package-lock.json
git status -s
