Hey Zach — we're putting RDS Proxy in front of Aurora to cut the ~5.5s DB connect cost on cold starts. Once it's in, the Lambda's db_endpoint env var will change from the Aurora reader endpoint to the proxy endpoint. Your generate_db_auth_token call in rds_iam_auth.py should just work as-is since it uses that env var — but could you check nothing else in the code assumes the direct Aurora hostname? No urgency, just flagging so it's not a surprise when we deploy.
