The zip is current. authorizer.zip was rebuilt in 5ca836c alongside the source change — verified the index.js inside it is byte-identical to authorizerSrc/index.js. main.tf:168 sets source_code_hash = filebase64sha256(...) on the zip, so Terraform detects the change and redeploys. Agreed the pre-built-artifact pattern is fragile long term; tracking a move to a CI-built artifact separately.
