PS C:\Users\T012sp\Git repo\real-time-insurance-infra> git diff aurora.tf
diff --git a/aurora.tf b/aurora.tf
index 45ed5e8..be0c455 100644
--- a/aurora.tf
+++ b/aurora.tf
@@ -82,12 +82,12 @@ resource "aws_rds_cluster" "aurora" {
   # IMPORTANT:
diff --git a/aurora.tf b/aurora.tf
index 45ed5e8..be0c455 100644
--- a/aurora.tf
+++ b/aurora.tf
@@ -82,12 +82,12 @@ resource "aws_rds_cluster" "aurora" {
   # IMPORTANT:
   # Aurora Serverless v2 uses engine_mode = "provisioned" (Serverless v1 used engine_mode="serverless")
   engine_mode = "provisioned"
-  
+
   master_username             = "kfb_aurora_admin"
   manage_master_user_password = true
 
PS C:\Users\T012sp\Git repo\real-time-insurance-infra> git diff lambdas.tf
fatal: ambiguous argument 'lambdas.tf': unknown revision or path not in the working tree.
Use '--' to separate paths from revisions, like this:
'git <command> [<revision>...] -- [<file>...]'
PS C:\Users\T012sp\Git repo\real-time-insurance-infra> git diff lambda.tf 
diff --git a/lambda.tf b/lambda.tf
index 0ace7c9..ced4f6d 100644
--- a/lambda.tf
+++ b/lambda.tf
@@ -1,40 +1,40 @@
 module "policy_graph_ingest_lambda" {
-  source      = "git::https://github.com/kfbmic/tf-module-lambda-function.git?ref=v1.1.0"
-  name = "${var.application_name}-${var.env_tier}-policy-graph-ingest-lambda"
-  runtime = "python3.13"
-  handler = "index.handler"
-  timeout     = 10
-  memory_size = 128
+  source                 = "git::https://github.com/kfbmic/tf-module-lambda-function.git?ref=v1.1.0"
+  name                   = "${var.application_name}-${var.env_tier}-policy-graph-ingest-lambda"
+  runtime                = "python3.13"
+  handler                = "index.handler"
+  timeout                = 10
+  memory_size            = 128
   code_managed_elsewhere = true
-  vpc_enabled = true
-  vpc_id      = var.vpc_id
-  subnet_ids  = var.lambda_subnet_ids
+  vpc_enabled            = true
+  vpc_id                 = var.vpc_id
+  subnet_ids             = var.lambda_subnet_ids
 
   tags = {}
 }
 
 module "soap_verification_lambda" {
-  source      = "git::https://github.com/kfbmic/tf-module-lambda-function.git?ref=v1.1.0"
-  name = "${var.application_name}-${var.env_tier}-soap-verification-lambda"
-  runtime = "python3.13"
-  handler = "main.lambda_handler"
-  timeout     = 10
-  memory_size = 512
+  source                 = "git::https://github.com/kfbmic/tf-module-lambda-function.git?ref=v1.1.0"
+  name                   = "${var.application_name}-${var.env_tier}-soap-verification-lambda"
+  runtime                = "python3.13"
+  handler                = "main.lambda_handler"
+  timeout                = 10
+  memory_size            = 512
   code_managed_elsewhere = true
-  vpc_enabled = true
-  vpc_id      = var.vpc_id
-  subnet_ids  = var.lambda_subnet_ids
+  vpc_enabled            = true
+  vpc_id                 = var.vpc_id
+  subnet_ids             = var.lambda_subnet_ids
   environment_variables = {
-               db_endpoint = aws_rds_cluster.aurora.reader_endpoint
-               db_user     = "lambda_svc"
+    db_endpoint = aws_db_proxy_endpoint.aurora_reader.endpoint
+    db_user     = "lambda_svc"
+  memory_size            = 512
   code_managed_elsewhere = true
-  vpc_enabled = true
-  vpc_id      = var.vpc_id
-  subnet_ids  = var.lambda_subnet_ids
+  vpc_enabled            = true
+  vpc_id                 = var.vpc_id
+  subnet_ids             = var.lambda_subnet_ids
   environment_variables = {
-               db_endpoint = aws_rds_cluster.aurora.reader_endpoint
-               db_user     = "lambda_svc"
+    db_endpoint = aws_db_proxy_endpoint.aurora_reader.endpoint
+    db_user     = "lambda_svc"
PS C:\Users\T012sp\Git repo\real-time-insurance-infra> git diff envs/dev.tfvars
diff --git a/envs/dev.tfvars b/envs/dev.tfvars
index 7d574ef..3f05c5a 100644
--- a/envs/dev.tfvars
+++ b/envs/dev.tfvars
@@ -9,4 +9,5 @@ replicas = 1
 serverless_v2_min_acu = 1 # was 0.5
 serverless_v2_max_acu = 2
 soap_api_domain_name  = "realtimeinsurance-soap-api-dev.devkyfb.com"
-soap_disable_execute_api_endpoint = true
\ No newline at end of file
+soap_disable_execute_api_endpoint = true
+rds_proxy_db_secret_arn = "REPLACE_WITH_ARN_FROM_CG"
\ No newline at end of file
