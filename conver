cache). Warm calls are fine (<1s). The team decided to skip the warmer/ACU-bump plan and switch dev Aurora from Serverless to Provisioned — matching what production will be — rather than optimize a config we're not keeping. RDS Proxy stays.
Next steps:

CG — figure out if Aurora can be switched Serverless → Provisioned in-place or needs cluster replacement + data reload.
You — wait for CG's answer, then update Terraform accordingly (small tfvars change if in-place, bigger effort if replace).
Zach — pause the warmup handler code change; no longer needed.
After switch — retest cold + warm and confirm sub-2s.
Loop in data team if a data reload is required.
