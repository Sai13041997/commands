Hi CG, the IAM login works now, but pgsi_ans04703015 can't connect to the database. The proxy log shows: permission denied for database "realtimeinsurance_dev". User does not have CONNECT privilege. Could you grant it CONNECT on realtimeinsurance_dev, plus access to the ods schema and tables? Please do the same for other envs(on realtimeinsurance_per and realtimeinsurance_stg ) and  pgsp_aps04703014 on realtimeinsurance_prod. 
