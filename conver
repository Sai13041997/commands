output "rds_proxy_application_username" {
  description = "Application DB username for IAM auth through the RDS Proxy. Null when no application user is configured."
  value       = local.app_auth_enabled ? var.rds_proxy_application_username : null
}

cd "C:\Users\T012sp\Git repo\tf-aws-aurorapostgres-infra"
Get-Content apps\realtimeinsurance\outputs.tf
