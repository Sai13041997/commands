Context

I'm working on a DEV SOAP API on AWS. Recently fixed a root-cause issue and now need to map the end-to-end request workflow and measure execution time at each hop.

Architecture

Client (mTLS)
  → Custom domain: realtimeinsurance-soap-api-dev.devkyfb.com
  → API Gateway REST API: q51dqcsrw5, stage "default"
  → Lambda: realtimeinsurance-dev-soap-verification-lambda
  → RDS Proxy: realtimeinsurance-dev-aurora-proxy (prx-05af88b21156e01ae)
  → Aurora PostgreSQL: realtimeinsurance_dev

Environment

AWS CLI profile: L2-Development, region us-east-1
Shell: PowerShell on Windows (use backtick line continuation, not backslash)
Lambda env vars: db_user=pgsi_ans00315015, db_endpoint=realtimeinsurance-dev-aurora-proxy.proxy-cx08iswmk3mr.us-east-1.rds.amazonaws.com, db_name=realtimeinsurance_dev
Log groups: /aws/lambda/realtimeinsurance-dev-soap-verification-lambda and /aws/rds/proxy/realtimeinsurance-dev-aurora-proxy
The Lambda already emits sequential log markers: SOAP parsed → VIN extracted → IAM token generated → DB connection established → cursor created → executing query

Recent change (for context)

The RDS Proxy previously had only the Aurora master-user secret registered, while the Lambda connects as pgsi_ans00315015. I added a second auth entry for the application secret (realtimeinsurance-NNrdwU) via Terraform/OpenTofu. Applied successfully; describe-db-proxies now shows both auth entries.

What I want

Commands to confirm the full workflow wiring — domain mapping, API resources and methods, the Lambda integration, Lambda config, and proxy targets.
Commands to measure execution time at each hop:
Lambda total duration and cold-start init time
Per-stage breakdown inside the Lambda, computed from the deltas between the existing log markers
API Gateway overhead, derived from Latency minus IntegrationLatency
Client-side timing for an mTLS curl request

Constraints

Give me runnable PowerShell commands, not descriptions
Flag any command that requires a value I have to fill in first, and tell me which prior command produces it
Note any prerequisite that has to be enabled (e.g. detailed CloudWatch metrics on the stage, X-Ray tracing)

One thing worth adding at the top if it applies: whether the Lambda invoke actually succeeds now. If it's still failing at the DB, say so explicitly — otherwise the other AI will hand you timing numbers for a broken path, and fast failures look deceptively good.
