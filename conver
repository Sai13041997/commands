output "rds_proxy_application_username" {
  description = "rds_iam application DB user for the RDS Proxy. Null when no application user is configured."
  value       = module.aurora.rds_proxy_application_username
}

tofu fmt -recursive
git add apps/realtimeinsurance/outputs.tf
git commit -m "Add description to rds_proxy_application_username output"
git push
