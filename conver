Copilot said:
The issue (plain English)

The SOAP Lambda is working, but when it tries to query Aurora through the RDS Proxy, Aurora rejects the login for:

Plain Text
1
pgsi_ans00315015
Show more lines

The exact error is:

Plain Text
1
PAM authentication failed for user "pgsi_ans00315015"
Show more lines
What we have proven so far
SOAP Flow

✅ API Gateway receives the SOAP request

✅ Lambda is invoked successfully

✅ SOAP XML is parsed correctly

✅ Lambda reaches the database layer

Lambda Configuration

✅ db_user = pgsi_ans00315015

✅ db_endpoint = realtimeinsurance-dev-aurora-proxy...

✅ Lambda code uses:

Python
1
generate_db_auth_token(...)
2
pg8000.connect(...)
Show more lines

Correct IAM authentication code.

IAM Permissions

✅ Lambda role has:

Plain Text
1
rds-db:connect
Show more lines

for:

Plain Text
1
pgsi_ans00315015
Show more lines

So IAM permissions are not the problem.

RDS Proxy

✅ Proxy is Available

✅ Proxy uses:

Plain Text
1
IAM Authentication = Required
Show more lines

✅ Proxy successfully connects to Aurora

Proxy log shows:

Plain Text
1
A TCP connection was established...
Show more lines

So networking is not the problem.

Aurora

✅ IAM Database Authentication = Enabled

✅ Aurora cluster is healthy

✅ Aurora admin secret exists

✅ Secret rotated on:

Plain Text
1
2026-08-20
Show more lines
CG Verification

CG confirmed:

the user still has the same privileges as before

So according to CG, nothing obvious changed with the role permissions.

What does NOT appear to be broken

❌ Not API Gateway

❌ Not Lambda code

❌ Not SGs / Networking

❌ Not RDS Proxy connectivity

❌ Not Lambda IAM permissions

❌ Not IAM Database Authentication being disabled

Where the problem is

The failure is happening inside Aurora authentication.

Aurora is rejecting:

Plain Text
1
pgsi_ans00315015
Show more lines

during IAM authentication.

That's why the Proxy log says:

Plain Text
1
PAM authentication failed for user "pgsi_ans00315015"
Show more lines
Strongest clue found tonight

The Aurora master secret was:

Plain Text
1
LastRotatedDate = 2026-08-20
2
LastChangedDate = 2026-08-20
Show more lines

which happened right around the Aurora migration/rebuild timeframe.

That doesn't prove the cause, but it's the biggest change we've found in the auth chain.

My current conclusion

The issue is not in AWS infrastructure anymore.

The issue is most likely one of:

Aurora's role definition for pgsi_ans00315015
Aurora IAM authentication configuration for that role
Something that changed in Aurora/proxy authentication after the Aug 20 secret rotation

The one thing we still have not directly verified is:

SQL
1
\du pgsi_ans00315015
Show more lines

inside the Aurora database.

That is the remaining missing piece. Everything else in the path has been validated.
