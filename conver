Hi CG, the IAM-only setup is working in dev, and we'd like perf and staging to match. Could you create pgsi_ users for the realtimeinsurance perf and staging clusters, with the same priv_service grants as the dev and prod ones? Just send the usernames when they're ready. No passwords needed, since they're IAM-only.
