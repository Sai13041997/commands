Yeah, technically but Secrets Manager has native RDS rotation. Enable it on the secret, and it handles both sides automatically generates a new password, updates the secret, and runs ALTER USER on Aurora in one atomic operation. Proxy picks up the new secret with no downtime. Without rotation enabled, yeah someone has to run ALTER USER every time. Not ideal. Want me to set up automated rotation so this is one-time setup and never a manual step again?
