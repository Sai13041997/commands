variable "manage_policy_separately" {
  type        = bool
  description = "When true, manage the REST API resource policy via a dedicated aws_api_gateway_rest_api_policy resource (reliable diffing). When false (default), use the legacy inline policy. Existing callers leave this off to preserve current behavior."
  default     = false
}
