variable "mtls_truststore_version" {
  type        = string
  description = "S3 object version ID of the mTLS truststore. Required when the truststore content is updated so AWS knows exactly which version to trust."
  default     = null
}



dynamic "mutual_tls_authentication" { 
  for_each = var.mtls_truststore_uri != null ? [1] : [] 
  content { 
    truststore_uri     = var.mtls_truststore_uri
    truststore_version = var.mtls_truststore_version
  } 
}


mtls_truststore_version = "PASTE_THE_VERSION_ID_HERE"

Hey [name], quick thing — testing the SOAP API for the KIVS integration, warm calls come back around 0.7s (fine), but cold calls are hitting ~9s because of Lambda + Aurora warm-up, which would blow past their 2s SLA on every occasional test. I'm looking at two options — provisioned concurrency on the Lambda (reliable but has ongoing cost) or an EventBridge ping every 5 min (cheap, not fully guaranteed). Just wanted to get another opinion before I go one way or the other — what do you think? Also noticed the Lambda opens a fresh DB connection per invocation, which is eating warm time too, but that's app-code side.
