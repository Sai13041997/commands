Here's everything left on the RDS proxy app-user work, grouped.
Merge and keep in sync
Merge the Aurora branch to main. Dev and prod run code that only exists on your branch, so another apply from main would revert it.
Merge the infra branch to main, once dev is proven with real traffic.
Reply to Copilot #2 on the Aurora PR. It flagged rds-db:connect on the proxy role as wrong. With end-to-end IAM, that grant is exactly what the proxy needs, so you can explain and resolve it.
Roll out to the remaining environments
Perf and staging. Waiting on CG to create pgsi_ans04703015 in those clusters. Then add one tfvars line each in the Aurora repo and apply. The infra repo picks it up automatically.
Prod infra apply, to switch the prod Lambdas to pgsi_aps04703014, after dev is proven.
The SOAP Lambda, in every environment. It only switches once the src pipeline publishes a new version and moves the live alias.
Clean up
Delete the old dev secret realtimeinsurance. Nothing references it anymore, and it holds a plaintext password for the retired pgsi_ans00315015.
Ask CG to rotate or retire the exposed credentials. That's the pgsp_aps04703014 password, which was shared in chat twice, and the old pgsi_ans00315015 user.
Remove the fallback in locals.tf, once every environment has an app user.
Related, but separate
Log groups, the permanent fix. Imports plus create_log_group = true, once retention is agreed.
PII in debug logs. The policy graph Lambda logs full customer payloads in prod.
The ones with real risk if left alone are 1, 7 and 8.
