variable "mtls_truststore_version" {
  type        = string
  description = "S3 object version ID of the mTLS truststore. Required when the truststore content is updated so AWS knows exactly which version to trust."
  default     = null
}



dynamic "mutual_tls_authentication" { 
  for_each = var.mtls_truststore_uri != null ? [1] : [] 
  content { 
    truststore_uri     = var.mtls_truststore_uri
    truststore_version = var.mtls_truststore_version
  } 
}


mtls_truststore_version = "PASTE_THE_VERSION_ID_HERE"

Yeah fair, provisioned concurrency does feel heavy — happy to avoid it if we can.
RDS Proxy is a good shout, hadn't thought about that. A big chunk of the cold time is the fresh DB connection + IAM token per invocation, so proxy would knock that down without needing any app-code changes.
Thinking I'd try RDS Proxy first, then a warmer if we still need it, and only go to provisioned concurrency as a last resort. Sound good?
 
