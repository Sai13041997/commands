variable "soap_disable_execute_api_endpoint" {
  description = "When true, disables the default execute-api endpoint for the SOAP API, enforcing traffic through the custom domain (mTLS) only."
  type        = bool
  default     = true
}


soap_disable_execute_api_endpoint = true

disable_execute_api_endpoint = var.soap_disable_execute_api_endpoint
