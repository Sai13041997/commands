      - name: Upload tfplan JSON artifact
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-json
          path: tfplan.json
          retention-days: 1
 
  opa-validate:
    needs: plan
    permissions:
      contents: read
    # NOTE: pinned to the same branch you were checking out manually before.
    # Move this to a release tag or @main once the tags policy work is merged.
    uses: kfbmic/opa-policy/.github/workflows/opa-validate.yml@feature/T012SP/CPE-227
    with:
      tfplan-artifact: tfplan-json
    secrets:
      cross_repo_app_id: ${{ secrets.CROSS_REPO_APP_ID }}
      cross_repo_key: ${{ secrets.CROSS_REPO_KEY }}
