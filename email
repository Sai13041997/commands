aws sso login --profile L2-UAT
$env:AWS_PROFILE = "L2-UAT"
aws sts get-caller-identity


tofu workspace select dev
tofu state list | Select-String -Pattern "aurora" -CaseSensitive:$false

tofu workspace list
